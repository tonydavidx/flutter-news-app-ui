import 'package:flutter/material.dart';

class ArticleByCategory extends StatelessWidget {
  const ArticleByCategory({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final List articlesCategories = [
      'Health',
      'Politics',
      'Art',
      'Food',
      'Science'
    ];
    return Text('data');
  }
}
